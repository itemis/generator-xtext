grammar <%= rootPackage %>.<%= languageName %> with org.eclipse.xtext.common.Terminals

generate <%= ePackageName %> "<%= ePackageNs %>"

Model:
    greetings+=Greeting*;
    
Greeting:
    'Hello' name=ID '!';
